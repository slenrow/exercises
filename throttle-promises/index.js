/**
 * Throttle promises implements a concurrency model limiting the execution of active parallel promises
 * to a given limit.
 *
 * @param limit - The concurrency limit
 * @param promises - The array of promises whose execution will be limited by `limit`
 */
module.exports = function throttlePromises(limit, promises) {

}
